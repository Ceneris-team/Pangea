fastapi
# HU17: los extras de uvicorn traen `websockets`, sin el cual uvicorn
# RECHAZA los handshakes de WebSocket (el endpoint /mapa-cliente/ws no
# funciona ni en local). `uvicorn` pelado no incluye ningún protocolo WS.
uvicorn[standard]
sqlalchemy
alembic
celery
redis
psycopg2-binary
pydantic
bcrypt==4.0.1  # fijado: passlib rompe con bcrypt>=4.1 (ver https://github.com/pyca/bcrypt/issues/684)
passlib[bcrypt]
pyjwt
cryptography
slowapi
python-dotenv
pydantic[email]
python-multipart  # HU06: FastAPI lo exige para leer multipart/form-data (vista previa del .dat de muestra)
pytest
httpx